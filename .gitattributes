# Auto detect text files and perform LF normalization
* text=auto

# Export ignore - exclude files from Composer package archives
# These files will NOT be included when someone runs 'composer require rajandangi/wp-gh-release-updater'

# Git files
/.gitattributes export-ignore
/.gitignore export-ignore
/.github export-ignore

# Development and CI files
/tests export-ignore
/phpcs.xml export-ignore
/phpstan.neon export-ignore
/rector.php export-ignore
/biome.json export-ignore
/composer.lock export-ignore
/.biomeignore export-ignore
/.distignore export-ignore

# Documentation (optional - remove these lines if you want to include docs)
/README.md export-ignore
/CHANGELOG.md export-ignore

# Node.js files (if you have them)
/package.json export-ignore
/package-lock.json export-ignore
/node_modules export-ignore

# Build/release files
/VERSION export-ignore
*.zip export-ignore
*.tar.gz export-ignore

# Note: We INCLUDE /vendor directory (with Composer autoloader) so the package works out-of-the-box
# The vendor dir only contains Composer's autoloader (~76KB) since we have no runtime dependencies

